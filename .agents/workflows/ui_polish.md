---
description: ARIA labels + responsive breakpoints and design system alignment
---

# UI Design Polish & Accessibility Sweep

1. Review the targeted UI components.
2. Inject necessary ARIA labels, roles, and accessible names for screen-reader compliance.
3. Validate and apply tailwind responsive breakpoints (`sm:`, `md:`, `lg:`) to ensure correct layout scaling.
4. Harmonize the component with the existing color palette (Blue/Green/Amber).
5. Output the cleaned and accessible UI layer.
