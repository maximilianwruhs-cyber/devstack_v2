---
description: Builds out the boilerplate for a new UI element
---

1. Ask the user for the name of the new component.
2. Create the main component file with a standard functional structure.
3. Automatically include a basic CSS/Tailwind wrapper, a props interface (if using TypeScript), and a companion test file.
